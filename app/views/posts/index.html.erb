<%= @user.name %><br>
<%= link_to 'New post', new_post_path %>
<br>
<% if @posts %>
  <% @posts.each do |post| %>
    <% if post.picture.attached? %>
      <img src="<%= url_for(post.picture) %>"><br>
    <% end %>
    <%= post.description %><br>
  <% end %>
<% end %>
